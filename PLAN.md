# Трек 1 (Mechanistic Interpretability): план решения

Версия 2. Учтены ревью Fable 5 и GPT-5.6-sol ([reviews/](reviews/)); v1 сохранена в git-истории.
Задание: [tasks/01-mechanistic-interpretability.md](../tasks/01-mechanistic-interpretability.md)

---

## 0. Требования и критерии

**Обязательное:**

| # | Требование задания | Где |
|---|---|---|
| R1 | Небольшая LM + векторы `v` для валидации | §2 |
| R2 | Метрики fluency и concept; dist-1/2/3 явно | §4 |
| R3 | Валидация `h+αv`, воспроизведение парето-картинки | §7, E1 |
| R4 | Денойзер не знает про валидационные `v` | §3 |
| R5 | Сравнение с обычным стирингом — только после обучения | §7 |
| R6 | Интервенция после серединного слоя | §2.1 |
| R7 | Отчёт + репозиторий + чекпойнт в открытый репозиторий | §8 |
| R8 | Идеи интереснее описанных, с доказательством | §5 |
| R9 | Анализ: почему и как метод работает | §6 |

**Критерии оценки (реконструкция).** Высокий вес: новизна идеи с доказательством (прямая цитата
авторов) и анализ механизма («вторая половина статьи»). Средний вес: корректность сравнений и
контроль утечки (видно из R4/R5), честность метрик (видно из акцента на двух осях), чистота кода и
воспроизводимость. Низкий вес, но бинарно: наличие чекпойнта в открытом репозитории.

**Главная ловушка.** Конструкция `h̃ = denoiser(h+αv)` дана авторами как *пример* («выше приведён лишь
пример»). Реализовать её буквально недостаточно: см. §5.1 — она имеет два дефекта, один из которых
делает точку `α=0` зависящей от произвольного `v`. Работа, которая это находит, объясняет и
исправляет, закрывает оба высоковесных критерия.

---

## 1. Преregистрация: гипотезы и предсказания

Записано до любых запусков на TEST. Числа — предсказания, не результаты.

**H1 (условная, не безусловная).** MSE-денойзер в пределе даёт `D(x) = E[h | h+ε = x]`, то есть
`D(x) − x = σ²∇log p_σ(x)`. Значит знак коррекции вдоль `v` определяется локальным градиентом
плотности, а не «оффманифолдностью» как таковой. В линейно-гауссовском приближении
`D(x) = μ + A(x−μ)`, `A = Σ(Σ+σ²I)⁻¹`, и **передача стиринга** равна

```
τ(v̂, σ) = 1 − σ² · v̂ᵀ(Σ + σ²I)⁻¹ v̂
```

Для собственного направления `Σ` с дисперсией `s`: `τ = s/(s+σ²)`. Предсказание: стирание
`1−τ` велико там, где дисперсия активаций вдоль `v̂` мала против `σ²`, и мало там, где велика.
Численно ожидаю `τ ∈ [0.3, 0.8]` для SAE-направлений при σ, выбранном на DEV.

**H2 (почему наивный денойзер не помогает).** При стирании `1−τ` наивный денойзер отдаёт часть
концепта в обмен на fluency, то есть даёт точку, близкую к `M1` при силе `τ·α`. Предсказание: фронт
`M3` лежит в пределах шума от фронта `M1`, а не выше него.

**H3 (метод).** Контрастная коррекция с сохранением направления (`M4`, §5.2) даёт ремонт, не платя
концептом → парето-доминирование над `M1` и `M3`. Предсказание: выигрыш по primary endpoint
+10…+30% относительно `M1`.

**H4 (механизм, замкнутая форма).** Основная часть ремонта — усадка энергии в низкодисперсионных
направлениях `Σ`. Следствие: `M5` (махаланобис-перенос, обучения не требует, §5.3) должен взять
заметную долю выигрыша `M4`. Предсказание: `M5` берёт ≥50% выигрыша `M4`.

**H5 (граница применимости).** Выигрыш `M4` падает с ростом дисперсии вдоль `v̂` (`v̂ᵀΣv̂`) и с
падением наклона `κ_tilt = ‖P⊥Σv̂‖/(v̂ᵀΣv̂)`. При `κ_tilt → 0` (`v̂` — собственный вектор `Σ`) `M4`
алгебраически вырождается в `M1` с перенастроенной силой, и выигрыша быть не должно. Предсказание:
ранговая корреляция выигрыша с `κ_tilt` положительна, |ρ_s| > 0.5 на 12 TEST-фичах.

**H6 (сильный метод).** Ремонт, обученный против замороженного downstream (`M6`, §5.4), побьёт
`M4`, потому что оптимизирует то, что нас волнует (не сломать вычисление верхних слоёв), а не
прокси-MSE на одном слое. Предсказание: `M6` > `M4` по primary endpoint, но не более чем в 1.5 раза.

Все шесть фальсифицируемы. Отрицательный исход любой из них — результат, а не провал; §6 показывает,
как именно он будет виден.

---

## 2. Сетап

### 2.1 Модель и точка интервенции

- **GPT-2 small** (124M, 12 слоёв, d=768), TransformerLens.
- Хук `blocks.6.hook_resid_post` — «после серединного слоя». Проверено эмпирически:
  `blocks.6.hook_resid_post` и `blocks.7.hook_resid_pre` — побитово один тензор
  (`max|Δ| = 0.0`), поэтому словарь SAE релиза `gpt2-small-res-jb` (обучен на `hook_resid_pre`)
  живёт ровно в базисе интервенции.
- Интервенция применяется ко всем позициям промпта и к каждому новому токену. С KV-кэшем это
  означает: на первом проходе хук видит все позиции промпта, далее — только новый токен. Это и есть
  корректная реализация «во всех позициях»; в коде это одна и та же функция хука.
- Позиции 0–1 исключены из оценки `μ, Σ, median‖h‖` и из интервенции: у GPT-2 норма резидуала на
  первых позициях — выброс (attention sink), он загрязняет статистики и шкалу `α`.

### 2.2 Направления и параметризация силы

- `v = W_dec[f]` (столбец декодера SAE), `v̂ = v/‖v‖`.
- Сила задаётся приращением концепт-координаты: `s = c · median‖h‖`, `α = s/‖v‖`. Все методы
  сравниваются при одинаковом `s`, а `M5` устроен так, что `v̂ᵀδ = s` выполняется по построению.
- Сетка `c ∈ {0, 0.5, 1.0, 1.5, 2.0, 3.0}`. Плотность около точки перегиба уточняется на DEV
  (fluency-коллапс — пороговый эффект, а не гладкий).

### 2.3 Отбор фич — автоматический, не вручную

Ручной подбор 6 «красивых» фич позволяет заявить только 6 case study. Поэтому отбор по объективному
правилу на корпусных активациях:

1. Для каждой фичи собираются топ-активирующие токены на held-out срезе корпуса.
2. Метрика лексической когерентности: доля массы активаций, приходящаяся на 10 самых частых
   токенов среди топ-200 активаций. Фича проходит, если доля > порога и частота активации в
   разумном коридоре (не мёртвая, не всеядная).
3. Из прошедших случайно (фиксированный сид) отбираются **12 TEST-фич** и **6 DEV-фич**.
4. Список токенов каждой фичи автоматически становится keyword-множеством для model-free
   concept-метрики; человеко-читаемое описание в одну фразу составляется по этим токенам и попадает
   в отчёт и в промпт судьи.

Это снимает вопрос «а вы не подобрали фичи под метод» и делает keyword-метрику воспроизводимой.

---

## 3. Разбиение и правило заморозки (R4 + защита от test-time-утечки)

Три непересекающихся множества индексов фич:

| Множество | Размер | Роль |
|---|---|---|
| `FIT` | остаток словаря | только синтез обучающих возмущений |
| `DEV` | 6 | выбор всех ручек: архитектура, σ/β, λ, η, промпт судьи, сетка `c` |
| `TEST` | 12 | открывается один раз, для финальных фронтов |

Буфер: любая фича с `|cos| > 0.3` к любому направлению из `DEV ∪ TEST` исключается из `FIT`.
В отчёт — таблица `max |cos(v_TEST, u_FIT)|`, чтобы утечку можно было проверить, а не принять на веру.

Промпты тоже разделены: `prompt-DEV` (10) и `prompt-TEST` (30), из разных документов корпуса; корпус
активаций — третий, непересекающийся шард.

**Правило заморозки.** Все инференс-ручки (`λ`, `η`, `σ` аналитического денойзера, температура,
top-p, длина, промпт судьи) фиксируются на DEV и записываются в `configs/frozen.yaml` с хешем
коммита **до** первого запуска на TEST. На TEST меняется только `c`. Это принципиально: свип `λ`
включает `λ=0`, то есть сам бейзлайн, поэтому фронт, построенный как объединение по `(c, λ)`,
доминировал бы бейзлайн тавтологически.

---

## 4. Метрики

### 4.1 Ось x — fluency

1. **Основная: log-PPL продолжения**, посчитанная только по сгенерированным токенам при условии
   промпта, под **Pythia-410m** — семейство, отличное и от GPT-2 (объект), и от Qwen (судья). Своя
   перплексия GPT-2 на своих же порождениях вырождена: сломанная модель уверена в своём мусоре.
2. **Робастность**: тот же замер вторым скорером (Qwen2.5-0.5B base). Если ранжирование методов
   меняется, это идёт в отчёт.
3. **dist-1/2/3** по Li et al. 2016: уникальные n-граммы, посчитанные на корпусном уровне и
   нормированные на общее число токенов, при фиксированной длине генерации. Честная оговорка:
   dist-n немонотонен — коллапс в повтор его снижает, деградация в белиберду повышает; показываем
   это на данных.
4. **Repetition rate** (доля повторяющихся 4-грамм) — ловит вторую моду поломки.

### 4.2 Ось y — concept

1. **Основная (model-free): keyword hit rate** по автоматически выведенному токен-множеству фичи,
   с исключением токенов, уже присутствующих в промпте.
2. **SAE feature activation**: средняя активация целевой фичи при прогоне сгенерированного текста
   обратно через модель+SAE. Циркулярность (тот же SAE, откуда взят `v`) признаётся явно, метрика
   вторичная.
3. **LLM-судья**: Qwen2.5-1.5B-Instruct, промпт из `persona_vectors`, оценка как
   **логпроб-взвешенное ожидание** по валидным числам при `max_tokens=1` — это устойчивее, чем
   парсинг свободного текста, и снимает проблему шумной шкалы у малой модели.
4. **Guard-метрика: prompt relevance** (косинус эмбеддингов промпта и продолжения). Нужна, потому
   что при большом `α` модель может просто игнорировать промпт и всё равно набирать concept score.

### 4.3 Валидация метрик (до основных запусков)

- Контроли: случайное направление той же нормы, `−v`, чужая TEST-фича, перемешанные метки концепта
  для судьи. Concept-метрика обязана отличать их от истинного `v`.
- Слепая ручная разметка стратифицированной подвыборки (~150 текстов, вслепую к методу и `α`):
  «текст связен / сломан» и «концепт присутствует / нет». Считается согласие с автоматическими
  метриками. Это тридцать минут работы, но снимает главный вопрос к обеим осям.

### 4.4 Primary endpoint (объявлен заранее)

**Keyword hit rate при выровненном fluency**: значение concept-метрики, интерполированное на
фиксированный бюджет log-PPL, равный log-PPL наивного стиринга `M1` при `c = 1.0`. Агрегация —
среднее по 12 TEST-фичам, доверительный интервал — **парный иерархический bootstrap** с
пересэмплированием на трёх уровнях (фича → промпт → сэмпл) и пересчётом фронта в каждой репликации.
Остальные метрики — вторичные, служат для проверки согласованности, а не для выбора победителя.

---

## 5. Методы

Обозначения: `x = h + αv`, `P = v̂v̂ᵀ`, `P⊥ = I − P`.

### 5.1 Два дефекта буквальной формулировки

**Дефект A: смешение двух ошибок.** `D(x) − x` содержит и «ремонт стиринга», и обычную ошибку
реконструкции `D(h) − h`, которая к стирингу отношения не имеет. Как следствие, при `α = 0` метод
`D(h)` уже портит чистую активацию, и нулевая точка фронта зависит от того, какой `v` мы выбрали.

**Дефект B: неверный замер стирания.** Величина `−⟨D(x)−x, v̂⟩/α` не определена при `α=0` и
загрязнена дефектом A. Правильная парная оценка передачи:

```
τ(α) = ⟨D(h + αv) − D(h), v⟩ / (α‖v‖²),      стирание = 1 − τ
```

Оба дефекта лечит одна и та же операция — вычитание базовой линии `D(h)`.

### 5.2 M4 — CDS: Contrastive Direction-preserving Steering-repair

```
Δ_steer = D(h + αv) − D(h) − αv          # чистый отклик денойзера на стиринг
h̃       = h + αv + λ · P⊥ · Δ_steer      # λ фиксирована на DEV
```

Свойства, которые делают конструкцию защитимой:
- при `α = 0` имеем `h̃ = h` **тождественно**, для любого `λ` и любого `v` — дефект A устранён;
- концепт-координата `v̂ᵀh̃` совпадает с наивным стирингом по построению, поэтому сравнение
  `M4` против `M1` идёт при равной силе концепта в этой координате;
- стоимость инференса — два прохода денойзера на токен (`D(x)` и `D(h)`, второй кэшируется по
  позиции) плюс одно скалярное произведение. ODE-шагов нет, в отличие от GLP.

**Условие вырождения, проверяемое до запусков.** В линейном случае, если `v̂` — собственный вектор
`Σ`, то `P⊥A v̂ = 0`, и `M4` алгебраически совпадает с `M1` при перенастроенной силе — выигрыша быть
не может. Поэтому до генерации считается `κ_tilt = ‖P⊥Σv̂‖/(v̂ᵀΣv̂)` по всем TEST-фичам (H5).

**Факторный диагностический грид на DEV** (вместо частичной реинъекции и проекции на `span{v,x}`,
убранных из плана): `h̃ = x + λΔ⊥ + γΔ∥`, `λ, γ ∈ [0,1]`. Он отвечает на вопрос ревью «а важна ли
`Δ∥` для fluency»: если оптимум имеет `γ > 0`, чистая ортогонализация противопоказана, и это надо
знать до TEST, а не после.

**Достаточна ли ортогональность к одному `v̂`.** Нет, в общем случае: сохранение
дифференцируемого concept score требует `∇C(h)ᵀδ = 0`, что совпадает с `v̂ᵀδ = 0` только при
`∇C ∥ v̂`. У SAE декодерное `W_dec[f]` и энкодерный функционал `W_enc[f]` не коллинеарны. Поэтому в
аблацию входит вариант с проекцией на `span{W_dec[f], W_enc[f]}`, и проверяется напрямую, совпадает
ли concept score `M4` и `M1` при равном `c`.

### 5.3 M5 — MTS: махаланобис-перенос стиринга (обучения не требует)

Минимальный по нарушению распределения сдвиг при заданном приращении концепт-координаты — решение
задачи `min δᵀΣ⁻¹δ` при `v̂ᵀδ = s`:

```
δ = s · Σ_γ v̂ / (v̂ᵀ Σ_γ v̂),     Σ_γ = (1−γ)Σ̂ + γ · (tr Σ̂/d) · I
```

Усадка `γ` обязательна: хвост спектра `Σ̂` по 2M токенов шумный. `γ` выбирается на DEV.
Концепт-координата совпадает с `M1` точно, по построению. Это одновременно и метод, и предел `M4`
при больших `λ`, что связывает всю линию работы в одну теорию: подсказка задания → винеровский
денойзер → CDS → махаланобис-оптимальный стиринг.

### 5.4 M6 — CTR: ремонт, обученный против замороженного downstream

Прокси-MSE на одном слое — не то, что нас волнует. Волнует: (а) верхние слои не сломаны, (б) концепт
доставлен. И то, и другое дифференцируемо, потому что верхняя половина GPT-2 small дешёвая.

Для чистой активации `h`, направления `u ∈ FIT`, малой безопасной силы `ε` и рабочей силы `β`
прогоняется замороженный суффикс слоёв 7–11, `F(·)` → финальные логиты:

```
z₀ = F(h),   z_ε = F(h + εu),   z_β = F(h + βu)
g  = (z_ε − z₀)/ε                                  # локальный причинный отклик концепта
a  = clip( ⟨z_β − z₀, g⟩ / (‖g‖² + δ) )
z* = z₀ + a·g                                      # teacher: сохранить то, что согласовано с g
```

Смысл: часть большого отклика, согласованная с *малым и ещё связным* стирингом, объявляется
полезной; нелинейная off-tangent часть — повреждением.

Обучается один residual MLP, `u` и нормированная `β` подаются через low-rank эмбеддинг:

```
h̃ = h + βu + R_θ(h + βu, u, β)
L  = T²·KL( softmax(z*/T) ‖ softmax(F(h̃)/T) ) + γ‖R_θ‖² + ξ‖R_θ(h, u, 0)‖²
```

Последний член — явный identity-контроль при нулевой силе. GPT-2 заморожен, градиент идёт только в
`R_θ`. Направления `DEV ∪ TEST` в обучении не участвуют.

Стоимость шага: три половинных прохода вперёд + один назад по шести блокам. При batch 8×128 это
~2 ГБ VRAM и умещается в ночь на 1660 Ti. Если полный KL по словарю окажется тяжёлым — top-k логиты
плюс Huber по финальному резидуалу.

### 5.5 M3′ — вариант «не менять структуру модели» (подсказка задания)

Подсказка задания про «дообучить существующие MLP» реализуется **на блоке 7, не 6**: MLP блока 6
считается *до* точки интервенции и по построению не может починить ещё не добавленный `αv`. LoRA
rank 8 на `blocks.7.mlp`, обученная на том же лоссе, одна конфигурация, без свипа. Позиционируется
как ответ на подсказку, а не как соревнующийся метод.

### 5.6 Денойзер и обучающий шум — сокращённый до минимума

- Архитектуры: **линейный** и **residual MLP 768→1536→768** (pre-LayerNorm, GELU, предсказывает
  невязку). Выбор по DEV. FiLM-кондиционирование и глубокие варианты убраны из плана.
- Шум: `x = h + β·u`, где `u` — либо изотропный гаусс, либо случайная разреженная комбинация
  столбцов `W_dec[FIT]`; `β` покрывает рабочий диапазон `‖αv‖`, включая большие значения.
  Аргумент «направление, общее для всей последовательности» **убран**: токенный MLP не видит
  соседние позиции, для него это статистически неотличимо от независимых направлений.
- Схема `t·h + (1−t)ε` из подсказки задания реализуется как обучение прямо на `x = h + s·u` с
  передачей известного нормированного `s`. Пытаться отобразить `t` из отношения норм на инференсе —
  train/inference mismatch: масштаб сигнала, среднее и SNR не совпадают.
- Аналитический винеровский денойзер `D(x) = μ + Σ(Σ+σ²I)⁻¹(x−μ)` остаётся, но с честной
  формулировкой: это posterior mean только при гауссовском `h` и изотропном шуме фиксированной
  дисперсии; для rank-1 стиринга он не оптимален и служит линейным бейзлайном и инструментом
  теории, а не доказательством механизма.

### 5.7 Полный список плеч и контролей

| ID | Метод | Роль |
|---|---|---|
| M0 | `h` | якорь |
| M1 | `h + αv` | фронт, который надо побить |
| M2 | `‖h‖·x/‖x‖` | скептический бейзлайн без обучения |
| M3 | `x + η(D(x) − x)`, `η` с DEV | буквальная формулировка задания, в лучшей DEV-конфигурации |
| M3′ | LoRA на `blocks.7.mlp` | ответ на подсказку задания |
| M4 | **CDS** | основной дешёвый метод |
| M5 | **MTS** | замкнутая форма, предел теории |
| M6 | **CTR** | сильный метод, обучен против downstream |

Контроли: `D(h)` (identity при `α=0`), `x+Δ∥`, `x+Δ⊥`, случайное направление, `−v`, чужая фича,
перемешанные метки судьи, `D(h)+αv` (сначала чистим, потом стирим), `D(h+βv)` с `β` под совпадение
concept score, DPD со случайным проектором вместо `v`. Два обучающих сида для `M4`-денойзера и `M6`.

Правило решения: метод лучше **только при парето-доминировании** и при непересекающихся
bootstrap-CI по primary endpoint.

---

## 6. Анализ (вторая половина работы)

1. **Передача стиринга** `τ(α)` с CI, для `M3` и `M4`, и сверка с формулой `τ = 1 − σ²v̂ᵀ(Σ+σ²I)⁻¹v̂`
   на аналитическом денойзере, где это теорема. → проверка H1, H2.
2. **Факторный грид `(λ, γ)`** на DEV: разделяет «ремонт» и «силу концепта» каузально. → обоснование
   выбора чистой ортогонализации или отказа от неё.
3. **Спектральное разложение**: энергия возмущения по собственным направлениям `Σ` до и после
   ремонта, для каждого метода. → проверка H4, объяснение механизма.
4. **Причинная декомпозиция A/C**: `A = ⟨z_m − z₀, g⟩/‖g‖²` (сохранённый полезный отклик) и
   `C = ‖z_m − z₀ − A·g‖/(|A|‖g‖)` (нелинейное повреждение) для всех методов. Утверждение, которое
   объединяет всю работу: при выровненном `A` метод выигрывает ровно настолько, насколько снижает
   `C`; проверяется корреляцией `ΔC` с `Δ log-PPL`.
5. **Предикторы выигрыша**: ранговая корреляция per-feature выигрыша с `v̂ᵀΣv̂` и с `κ_tilt`. →
   проверка H5, и это единственный способ отличить «метод работает» от «повезло на этих фичах».
6. **Разбор отказов**: фичи, где `M4`/`M6` не выигрывают, с объяснением через п.5, и качественные
   примеры текстов.
7. **Латентность** (токенов/с) всех плеч + число параметров. Дешевизна — часть заявки.

Убрано из плана как малополезное на единицу времени: гиперобъём фронта, kNN-расстояние по 2M точек,
послойная трассировка всех слоёв 7–11 (достаточно спектра и двух downstream-чекпойнтов).

---

## 7. Порядок работ

- **E0** ✅ Инфраструктура: venv (torch 2.13+cu126, sm_75 проверен), GPT-2 + SAE загружены,
  тождество хуков проверено, дамп 2M активаций, `μ, Σ, median‖h‖`, промпты.
- **E1** Отбор фич автоматическим правилом, разбиение `FIT/DEV/TEST`, таблица утечки,
  `κ_tilt` и `v̂ᵀΣv̂` по TEST-фичам (записываются как предсказания). Бенчмарк пропускной способности
  генерации / PPL / судьи на этой GPU — сетка фиксируется по измеренному throughput, а не по оценке.
- **E2** Валидация метрик: контроли §4.3, слепая ручная разметка, корреляции. Фиксация `M1`, `M2` на
  TEST. Заморозка бейзлайнов.
- **E3** Обучение: денойзеры (линейный, MLP; гаусс, разреженный словарь) на `FIT`, выбор ручек на
  DEV, факторный грид `(λ, γ)`, `η`, `γ_shrink`. Обучение `M6`. Запись `configs/frozen.yaml`.
- **E4** Один прогон на TEST: все плечи, фронты, primary endpoint, bootstrap.
- **E5** Анализ §6.
- **E6** Отчёт, HTML, артефакт чекпойнта и model card.

Между E2 и E4 файлы результатов бейзлайнов не перезаписываются.

---

## 8. Артефакты

```
01-mech-interp/
  README.md            установка и точные команды воспроизведения
  REPORT.md            отчёт
  report.html          версия для чтения
  PLAN.md              этот файл (преregистрация)
  reviews/             внешние ревью плана
  kb/                  база знаний: 11 заметок + 10 PDF первоисточников
  configs/             features.yaml, frozen.yaml, grids.yaml
  src/                 common, activations, features, steering, denoiser, noise,
                       train_denoiser, train_ctr, generate, metrics, analysis
  scripts/             run_e0..run_e6
  results/             csv + png
  artifacts/           чекпойнт + model card + push_to_hf.py
```

Чекпойнт готовится полностью (веса, карточка, скрипт публикации). Сама публикация в открытый
репозиторий — внешнее действие, поэтому выполняется владельцем аккаунта одной командой; в README
она вынесена отдельным шагом.

## 8а. Правки v2.1 после ревью Kimi K3

**Гейт по энергетике шума (иначе H1 недоказуема).** Исход теста H1 управляется выбором `σ`: если
`σ√d ≪ ‖s·v̂‖`, вход денойзера лежит вне обучающего шарика и «стирание» — артефакт экстраполяции
MLP, а не свойство posterior mean; если `σ√d ≳ ‖s·v̂‖`, стирания почти нет. Поэтому во всех рукавах
энергия возмущения сэмплируется **из одного и того же распределения**, покрывающего диапазон
развёртывания, и меняется только структура направления. Рукав с фиксированным `σ` остаётся как
аблация, демонстрирующая сам артефакт: график `τ(α)` при трёх разных `σ_train` — это прямое
доказательство, что вопрос про экстраполяцию задан и закрыт.

**Гейт валидности судьи (до основных запусков).** Judge-метрика допускается к использованию, только
если по каждой фиче AUC разделения `M0` против `M1` при максимальном `c` превышает 0.9, и ранговая
корреляция судьи с ручной разметкой ~100 текстов значима. Фичи, не прошедшие гейт, идут только с
keyword- и SAE-метриками, с явной оговоркой в отчёте. Primary endpoint и так построен на
model-free keyword-метрике, поэтому провал гейта работу не рушит.

**M7 — FSR: Feature-Surgery Repair (обучения не требует, интерпретируем по построению).**
SAE слоя 7 уже в стеке, новых компонентов ноль:

```
z  = enc(h + s·v̂)
z'_j = μ_j + softclamp(z_j − μ_j, k·σ_j)   для всех j вне {target ∪ буфер}; target не трогаем
h̃  = dec(z') + (x − dec(z))               # error passthrough: damage@s=0 равен нулю тождественно
```

`μ_j, σ_j` — статистики активаций фичи `j` на том же корпусном дампе; `k ≈ 3…4` выбирается на DEV.
Гипотеза: поломка fluency опосредована небольшим набором «захваченных» не-target фич, а не диффузным
сдвигом. Кламп возвращает вход слоёв 7–11 в естественную оболочку, сохраняя драйв целевой фичи.
Фальсифицируемое следствие: выигрыш пропорционален числу аномальных не-target фич.

Почему это важнее геометрических рукавов для данного трека: список наиболее клампируемых фич — это
**названный механизм поломки** на уровне интерпретируемых признаков, а не спектральный намёк. Раздел
«почему и как работает» получает feature-level доказательства.

**Kill-switch перед FSR.** Сначала измеряется ошибка roundtrip `x → enc → dec → x` на чистых
активациях: если damage@s=0 от реконструкции SAE сопоставим с поломкой от стиринга, идея снимается
до вложений. Error passthrough в формуле выше это в основном лечит, но проверка обязательна и стоит
15 минут.

**Проверка сдвига распределения на роллаутах.** Денойзер обучен на корпусных активациях, а на
инференсе видит активации, порождённые уже сдвинутым контекстом. Замеряется расхождение (норма,
махаланобис) между корпусными активациями и активациями с роллаутов; при существенном расхождении в
обучающую выборку добавляется смесь корпус + генерации. Это известное смещение, и оно фиксируется в
отчёте независимо от исхода.

**Приоритет реализации (защита от оверскоупа).** Обязательный минимум: `M0–M3`, `M4` (CDS),
`M5` (MTS), `M7` (FSR) — все дешёвые, ни один не требует ночи. `M6` (CTR) и `M3′` (LoRA) —
растяжимая часть, делаются только если после полного прогона обязательного минимума остаётся ночь.

Итоговая линия повествования отчёта: диагностика передачи `τ` → дешёвый геометрический ремонт (CDS)
→ его замкнутая форма (MTS) → вопрос «что именно сломано, а не где» (FSR, механизм на уровне фич)
→ верхняя граница обучением против downstream (CTR).

## 9. Бюджет

Сетка фиксируется после бенчмарка в E1. Ориентир: 12 фич × 6 значений `c` × 30 промптов × 1 сэмпл ×
8 плеч ≈ 17k генераций по 48 токенов. Генерация GPT-2 small батчами — минуты; PPL-скорер 410M —
десятки минут; судья 1.5B — самая тяжёлая часть, при нехватке времени судейство идёт по
стратифицированной подвыборке 3–5k текстов с CI, а полная сетка считается по двум дешёвым
concept-метрикам. Обучение денойзера — минуты, `M6` — одна ночь. Итого две ночи с запасом.
