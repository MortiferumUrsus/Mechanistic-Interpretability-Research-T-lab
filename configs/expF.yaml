# Grid for the `shared` arm's kappa under the "entropy compensation" sweep: for each value the
# driver rewrites configs/expA.yaml:kappa_shared and reruns generation/scoring at that setting.
kappa_grid: [-0.75, -0.5, -0.25, 0.25, 0.5, 0.75, 1.0]
