* text=auto
# The annotation package is covered by SHA256SUMS.txt computed over exact bytes;
# it must survive any clone byte for byte, so no end-of-line conversion is allowed.
evaluation/ai_annotation/** -text
# These two files are frozen records. Keep their exact bytes in every clone.
TASK.md -text
PLAN.ru.md -text
*.pt -text
*.png -text
*.pdf -text
# The annotation validator hashes these three files as critical sources with the exact bytes they had
# when the package was frozen (CRLF line endings). Keep them byte-identical on every platform.
configs/features.yaml -text
results/gen_r3.jsonl -text
results/gen_r3_ctrl.jsonl -text
